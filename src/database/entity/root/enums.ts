export enum Status {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE',
}

export enum GenderType {
    MALE = 'MALE',
    FEMALE = 'FEMALE',
    RATHER_NOT_SAY = 'RATHER_NOT_SAY',
    ANY = 'ANY',
}

export enum Roles {
    ADMIN = 'ADMIN',
    EMPLOYEE = 'EMPLOYEE',
}

export enum RoleNames {
    ADMIN = 'Admin',
    EMPLOYEE = 'Employee',
}