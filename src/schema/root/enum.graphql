enum Status {
    ACTIVE
    INACTIVE
}

enum Gender {
	ANY
    MALE
    FEMALE
    RATHER_NOT_SAY
}

enum PaymentStatus {
    PENDING
    PENDING_PAYMENT
    SUCCESS
    FAILURE
	REFUNDED
}

enum GlobalError {
    EXCEPTION
    NOT_ALLOWED
    ALREADY_EXISTS
    VALIDATION_ERROR
    RECORD_NOT_FOUND
    INVALID_FIELD_FORMAT
    INVALID_INPUT
    INTERNAL_SERVER_ERROR
    ALREADY_DELETED
    REQUIRED_FIELDS_MISSING
    PAYMENT_ERROR
    NOT_FOUND
    EMAIL_ALREADY_EXISTS
    WEAK_PASSWORD
    ROLE_NOT_FOUND
    COMPANY_NOT_FOUND
}