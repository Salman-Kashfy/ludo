enum Status {
    ACTIVE
    INACTIVE
}

enum Gender {
	ANY
    MALE
    FEMALE
    RATHER_NOT_SAY
}

enum PaymentStatus {
    PENDING
    PENDING_PAYMENT
    SUCCESS
    FAILURE
	REFUNDED
}

enum GlobalError {
    NOT_ALLOWED
    ALREADY_EXISTS
    RECORD_NOT_FOUND
    VALIDATION_ERROR
    INVALID_FIELD_FORMAT
    INTERNAL_SERVER_ERROR
    REQUIRED_FIELDS_MISSING
}