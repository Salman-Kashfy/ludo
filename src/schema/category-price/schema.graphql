type CategoryPrice {
    uuid: ID!
    categoryId: Int!
    price: Float!
    unit: CategoryPriceUnit!
    duration: Int!
    currencyName: String!
    freeMins: Int
    
    # Relations
    category: Category!
}

enum CategoryPriceUnit {
    minutes
    hours
}