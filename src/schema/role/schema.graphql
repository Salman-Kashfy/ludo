type Role {
    id: ID!
    name: String!
    status: Status!
}

enum Roles {
	ADMIN
	EMPLOYEE
}

extend type Query {
	roles: RolePayload! @requireAuth @requirePermissions(permissions: ["lead:view"])
}

type RolePayload {
	list: [Role]
}