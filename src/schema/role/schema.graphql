type Role {
    id: ID!
    name: String!
    status: Status!
}

enum Roles {
	SUPER_ADMIN
	BRAND_ADMIN
	GYM_ADMIN
	PT_ADMIN
}

extend type Query {
	roles: RolePayload! @requireAuth @requirePermissions(permissions: ["lead:view"])
}

type RolePayload {
	list: [Role]
}